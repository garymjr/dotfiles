version = 1
id = "weekly-permission-rule-review"
kind = "cron"
name = "Weekly Permission Rule Review"
prompt = "Use the $rule-maintenance skill to review and maintain `$CODEX_HOME/rules/default.rules` from recent Codex thread evidence. Follow the skill workflow instead of doing manual log counting: run `python3 `$CODEX_HOME/skills/rule-maintenance/scripts/rule_maintenance.py --days 7` first, read `$CODEX_HOME/rules/default.rules` directly before any edit, and use the report as evidence rather than an automatic patch. Add only safe, narrow, non-duplicate `prefix_rule` entries that meet the skill criteria, and prefer leaving the file unchanged over adding broad or borderline rules. If reviewing stale rules, run the 90-day report separately and remove only rules that satisfy the skill's stale-rule criteria. After any edit, rerun the report and read back the changed stanza(s); if no edit is warranted, say why. Keep output concise with the exact commands run, evidence window, candidate rules added or skipped, files changed, and unresolved uncertainty. Do not print secret-bearing values, PII, credentials, production data, or broad environment dumps."
status = "ACTIVE"
rrule = "RRULE:FREQ=WEEKLY;BYHOUR=9;BYMINUTE=0;BYDAY=FR"
model = "gpt-5.4-mini"
reasoning_effort = "medium"
execution_environment = "local"
cwds = ["/Users/gmurray/.codex"]
created_at = 1780418095479
updated_at = 1781109447997
